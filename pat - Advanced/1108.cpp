#include<iostream>

using namespace std;

int main() {
	int n;
	int i, j;
	double sum = 0; // 合法数据之和
	int num = 0;//合法数据的个数
	string str;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> str;
		int f = 0;
		for (j = 0; j < str.length(); j++) {
			if (str[j] >= '0' && str[j] <= '9') {

			}
		}
	}
	return 0;
}